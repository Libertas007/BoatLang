package com.libertas.parser.nodes;

import com.libertas.generics.Region;

public abstract class DefinitionNode extends Node {
    public DefinitionNode(Region region) {
        super(region);
    }
}

